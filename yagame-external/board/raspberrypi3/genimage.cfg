image boot.vfat {
  vfat {
  }
  size = 32M
}

image sdcard.img {
  hdimage {
  }

  partition boot {
    in-partition-table = "yes"
    image = "boot.vfat"
    partition-type = 0xC
    bootable = "true"
  }

  partition rootfs {
    in-partition-table = "yes"
    image = "rootfs.ext4"
  }
}
